name: Promote to UAT

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Promote and Deploy to UAT Org
    runs-on: ubuntu-latest
    environment: uat
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: uat

      - name: Merge qa into uat
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git merge origin/qa --ff-only
          git push origin uat

      - name: Salesforce Setup
        uses: ./.github/actions/sf-setup
        with:
          auth-url: ${{ secrets.SF_AUTH_URL }}

      - name: Delta Deploy to UAT
        uses: ./.github/actions/sf-delta-deploy
        with:
          environment: uat
          test-class: AccountServiceTest

      - name: Tag successful deployment
        run: |
          git tag -f deployed/uat
          git push origin refs/tags/deployed/uat --force

      - name: Notify Teams — UAT ready for sign-off
        if: always()
        continue-on-error: true
        env:
          TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
          STATUS: ${{ job.status }}
          ACTOR: ${{ github.actor }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          [ -z "$TEAMS_WEBHOOK_URL" ] && echo "TEAMS_WEBHOOK_URL not configured — skipping notification." && exit 0
          COLOR=$([ "$STATUS" = "success" ] && echo "0070C0" || echo "FF0000")
          TITLE=$([ "$STATUS" = "success" ] && echo "UAT Deployment Succeeded — Ready for Sign-Off" || echo "UAT Deployment Failed")
          curl -s -H "Content-Type: application/json" -d "{
            \"@type\": \"MessageCard\",
            \"@context\": \"http://schema.org/extensions\",
            \"themeColor\": \"$COLOR\",
            \"summary\": \"$TITLE\",
            \"sections\": [{
              \"activityTitle\": \"$TITLE\",
              \"facts\": [
                {\"name\": \"Triggered by\", \"value\": \"$ACTOR\"},
                {\"name\": \"Commit\", \"value\": \"${{ github.sha }}\"},
                {\"name\": \"Pipeline Run\", \"value\": \"$RUN_URL\"}
              ]
            }]
          }" "$TEAMS_WEBHOOK_URL"
